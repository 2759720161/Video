/** Whether a completed item should continue with the next item in its temporary playback queue. */
export const PREFS_KEY_PLAYER_AUTO_NEXT: string = 'playerAutoNext'
export const PLAYER_AUTO_NEXT_DEFAULT: boolean = true
